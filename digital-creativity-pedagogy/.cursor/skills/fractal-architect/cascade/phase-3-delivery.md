# Phase 3: Delivery And Client

Cold-session prompt:

> Consume the release-specific public manifest in the client. Render the exact
> formula, short explanation, Chicago reference, authorship/date, and hash in
> the visual caption or real figcaption as appropriate to the media element.
> Create local Ollama embeddings only from public-safe records and store them
> privately. Verify client failure when an indexed asset lacks a manifest record.

Dependencies: Phases 1-2.

Gate: rendered public output contains no private source-system metadata.
