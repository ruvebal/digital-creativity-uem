# Phase 1: Contract

Cold-session prompt:

> Define or revise the public figure-caption manifest and private provenance
> receipt for formula-governed assets. The public contract must include
> authorship email, ISO date, UUID, SHA-256, title, description, source/delivery
> asset names, every formula's display text, LaTeX, explanation, and
> evaluator-safe Chicago citation. Prohibit all local evidence identifiers from
> the public contract. Return schema examples and invalid cases only.

Gate: public and private fields have no overlap that leaks private provenance.
