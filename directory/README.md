# Field directory (Digital Creativity)

Data-as-code. Edit YAML here → `npm run hydrate` → `/directory/en/`.

Seeded from `digital-creativity-pedagogy/grounding/profield-authorities-directory.md`
(student-safe summaries only; internal dispute flags stay in pedagogy, not here).

See `SCHEMA.md` for the entry contract (shared shape with CT).
