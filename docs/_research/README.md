# Research corpus — NOT PUBLISHED

Excluded from the Jekyll build (`_config.yml` → `exclude: _research`).

**Canonical authority set for this course lives outside `docs/`:**

→ [`../../digital-creativity-pedagogy/grounding/`](../../digital-creativity-pedagogy/grounding/)

That directory mirrors the `frontend-pedagogy/` pattern from Web Atelier: field map, authorities directory, seed lists — referenceable by lessons and by the vShowroom synergy — without being shipped to GitHub Pages.

## Update protocol

1. Prefer editing / re-syncing files under `digital-creativity-pedagogy/grounding/`
2. Extract publishable excerpts into `docs/lessons/` only after ethical review
3. Never publish student design work or raw Ahmes extractions here
